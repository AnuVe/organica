<%- include('../partials/header') %>

<h1><%= plant.name %> </h1>

<img src="<%= plant.image %>">

<p>
    <a href="/plants/<%= plant._id %>/comments/new">Add new Comment</a>
</p>

<% plant.comments.forEach(function(comment){ %>
    <p><%= comment.author %> - <%= comment.text %> </p>
<% }) %>

<%- include('../partials/footer') %>